{
  "name": "roombelt",
  "version": "0.3.0",
  "scripts": {
    "dev:frontend": "cd frontend && cross-env PORT=3001 NODE_PATH=./src BROWSER=none npm run dev",
    "dev:backend": "cross-env PORT=3000 NODE_ENV=development nodemon --watch backend index.js",
    "prestart": "cd frontend && npm run build",
    "start": "node index.js",
    "install:frontend": "cd frontend && npm i",
    "install:backend": "cd backend && npm i",
    "postinstall": "npm run install:frontend && npm run install:backend"
  },
  "private": true,
  "devDependencies": {
    "cross-env": "^5.1.4",
    "http-proxy-middleware": "^0.19.0",
    "nodemon": "^1.17.3"
  }
}
